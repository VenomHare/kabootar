export interface DisplayMail { 
    id: string,
    from : string,
    to: string,
    subject: string,
    sent_at: string
}
export interface MailData { 
    id: string,
    from : string,
    to: string,
    subject: string,
    body: string,
    sent_at: string
}